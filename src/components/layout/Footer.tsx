function Footer() {
  return (
    <footer className="fixed bottom-0 w-full text-center py-4 z-50 bg-gradient-to-t from-white to-white/20 backdrop-blur-sm border-t border-gray-200">
      <p
        className="text-lg italic text-gray-600 font-semibold text-center"
        style={{ fontFamily: "'Manrope', sans-serif" }}
      >
        Always leveraging modern technologies to deliver solutions with maximum
        impact
      </p>
    </footer>
  );
}

export default Footer;
